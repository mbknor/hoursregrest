#The client-program downloads this file from github.
#then it reads the versionString. if version string online is larger then
#the versionstring embeded in program, the user is alerted that a newer verion of the program can
#be downloaded

currentVersion=1.3